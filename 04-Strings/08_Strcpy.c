#include <stdio.h>
#include <string.h>

  int main() {
  char *st = "Surya";
  int a = strlen(st);
  printf("The length of string st is %d\n",  a );
  return 0;
}

// output:
// The length of string st is 5
